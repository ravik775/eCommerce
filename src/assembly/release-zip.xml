<!--
  ADR-0011 (doc/adr/ADR-0011-artifact-packaging-zip.md): each service is
  packaged as its existing Spring Boot executable JAR, wrapped into a
  release ZIP containing the JAR, a config template, and a start script.
  Shared by every runnable service via maven-assembly-plugin (bound in the
  root pom's pluginManagement) so this descriptor lives in exactly one place.
-->
<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>release</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>

    <files>
        <file>
            <source>${project.build.directory}/${project.build.finalName}.jar</source>
            <outputDirectory>/</outputDirectory>
            <destName>${project.artifactId}.jar</destName>
        </file>
    </files>

    <fileSets>
        <fileSet>
            <directory>${maven.multiModuleProjectDirectory}/packaging</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>start.sh</include>
                <include>application.yml.template</include>
            </includes>
            <filtered>true</filtered>
        </fileSet>
    </fileSets>
</assembly>
