package org.bgm.catalogservice.model;

/**
 * DRAFT: created but not yet visible in search/browse — the "one addition"
 * to Phase 8's provider feature (deliberately not a full moderation
 * queue/approval role, per the same documented-simplification pattern
 * as this project's manually-granted role model and simulated payment
 * processor). LISTED: visible.
 */
public enum ProductStatus {
    DRAFT,
    LISTED
}
